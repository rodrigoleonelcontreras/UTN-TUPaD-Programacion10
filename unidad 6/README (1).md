
# TP Funciones - Python

Este repositorio contiene las soluciones para el Trabajo Práctico de funciones en Python.

## Contenido

- **TP_funciones.py**: archivo con todas las funciones solicitadas.
- Cada función resuelve uno de los ejercicios del práctico.

## Lista de funciones

1. `imprimir_hola_mundo()`
2. `saludar_usuario(nombre)`
3. `informacion_personal(nombre, apellido, edad, residencia)`
4. `calcular_area_circulo(radio)`
5. `calcular_perimetro_circulo(radio)`
6. `segundos_a_horas(segundos)`
7. `tabla_multiplicar(numero)`
8. `operaciones_basicas(a, b)`
9. `calcular_imc(peso, altura)`
10. `celsius_a_fahrenheit(celsius)`
11. `calcular_promedio(a, b, c)`

## Uso

Puedes importar este archivo y llamar a las funciones desde tu propio código:

```python
from TP_funciones import calcular_promedio
print(calcular_promedio(4, 7, 10))
```

## Autor
Generado automáticamente.
