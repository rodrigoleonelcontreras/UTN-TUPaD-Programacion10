
# TP 7 – Recursividad

Este proyecto contiene todas las funciones recursivas solicitadas en el práctico.

## Funciones incluidas

### 1. factorial(n)
Calcula el factorial de un número usando recursión.

### 2. mostrar_factoriales(hasta)
Imprime los factoriales desde 1 hasta el número ingresado.

### 3. fibonacci(n)
Devuelve el valor en la posición n de la serie de Fibonacci.

### 4. mostrar_fibonacci(hasta)
Genera la serie completa hasta la posición indicada.

### 5. potencia(base, exp)
Calcula una potencia usando recursión según la fórmula n^m = n * n^(m-1).

### 6. decimal_a_binario(n)
Convierte un número decimal a binario utilizando recursividad.

### 7. es_palindromo(palabra)
Verifica si una palabra es un palíndromo sin usar slicing inverso.

### 8. suma_digitos(n)
Suma los dígitos de un número sin convertirlo a string, usando (% y //).

### 9. contar_bloques(n)
Calcula la cantidad total de bloques para formar una pirámide de niveles.

### 10. contar_digito(numero, digito)
Cuenta cuántas veces aparece un dígito dentro de un número positivo.

## Ejemplo de uso

```python
from TP7_recursividad import factorial, decimal_a_binario

print(factorial(5))            # 120
print(decimal_a_binario(10))   # 1010
```

## Autor
Generado automáticamente según el práctico enviado.
